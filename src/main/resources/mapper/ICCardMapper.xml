<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smart.canteen.mapper.IcCardMapper">

    <select id="getByNo" resultType="com.smart.canteen.vo.CardVo">
        select no, employee_name as empName, employee_no as empNo, current_balance as currentBalance
        from ic_card
        where no = #{no}
          and deleted = 0
        limit 0,1
    </select>
</mapper>
